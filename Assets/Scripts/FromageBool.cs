using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FromageBool : MonoBehaviour
{

    public bool fromageExiste;
    // Start is called before the first frame update
    void Awake()
    {
        fromageExiste = false;
    }
}
